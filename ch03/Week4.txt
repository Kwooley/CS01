
• The summary of the last week

    - cin object for input stream
    - Expressions
    - Type Conversion
        implicit - mixed operands ( int + float)
        explicit - static_cast<int>( )

    - += /= *= ..
    - Formatting output
        <iomainip> 
        setw(), setprecision(), fixed, showpoint, ..


    - cin.get(), getline()

    getline() for the string data Type
    string name;
    cin >> name;

    getline(cin, name)
    
    - Library Functions
    